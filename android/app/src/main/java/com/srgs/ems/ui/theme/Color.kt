package com.srgs.ems.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary — Sky Blue → Indigo gradient ──────────────────────────────────────
val Primary      = Color(0xFF6366F1)   // indigo-500
val PrimaryLight = Color(0xFF818CF8)   // indigo-400
val PrimaryDark  = Color(0xFF4F46E5)   // indigo-600

// Gradient accent (used in headers, FABs, avatar rings)
val GradientStart = Color(0xFF6366F1)  // indigo
val GradientEnd   = Color(0xFF0EA5E9)  // sky-500

// ── Secondary ─────────────────────────────────────────────────────────────────
val Secondary      = Color(0xFFF472B6)  // pink-400
val SecondaryLight = Color(0xFFFBCFE8)  // pink-100

// ── Surfaces ──────────────────────────────────────────────────────────────────
val Background   = Color(0xFFF1F5F9)   // slate-100
val Surface      = Color(0xFFFFFFFF)
val SurfaceLight = Color(0xFFF8FAFC)   // slate-50
val SurfaceCard  = Color(0xFFFFFFFF)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFF0F172A)  // slate-900
val TextSecondary = Color(0xFF475569)  // slate-600
val TextMuted     = Color(0xFF94A3B8)  // slate-400

// ── Borders & Dividers ────────────────────────────────────────────────────────
val Border = Color(0xFFE2E8F0)         // slate-200

// ── Semantic ──────────────────────────────────────────────────────────────────
val Danger      = Color(0xFFEF4444)    // red-500
val DangerLight = Color(0xFFFEE2E2)    // red-100
val Success     = Color(0xFF10B981)    // emerald-500
val SuccessLight = Color(0xFFD1FAE5)   // emerald-100
val Warning     = Color(0xFFF59E0B)    // amber-500
val WarningLight = Color(0xFFFEF3C7)   // amber-100

// ── Stat card accent colours ───────────────────────────────────────────────────
val AccentBlue    = Color(0xFF0EA5E9)  // sky-500
val AccentPurple  = Color(0xFFA855F7)  // purple-500
val AccentGreen   = Color(0xFF10B981)  // emerald-500
val AccentOrange  = Color(0xFFF97316)  // orange-500
val AccentRose    = Color(0xFFF43F5E)  // rose-500
